// CategoriesPage — Category tree management

import { GroupBox, VerticalBox, HorizontalBox, StandardListView, Button, LineEdit, Palette } from "std-widgets.slint";
import { Page } from "../components/page.slint";
import { SearchBar } from "../components/search_bar.slint";
import { AppState, CategoryItem } from "../globals/app_state.slint";

export component CategoriesPage inherits Page {
    property <bool> show-form: false;

    title: "Categories";
    description: "Organize products into hierarchical categories.";

    SearchBar {
        vertical-stretch: 0;
        placeholder: "Search categories...";
        action-text: "Add Category";
        action-clicked => { root.show-form = !root.show-form; }
    }

    if root.show-form : GroupBox {
        vertical-stretch: 0;
        title: "New Category";

        VerticalBox {
            HorizontalBox {
                spacing: 8px;

                VerticalBox {
                    Text { text: "Name"; }
                    name-input := LineEdit { placeholder-text: "Category name"; }
                }

                VerticalBox {
                    Text { text: "Description"; }
                    desc-input := LineEdit { placeholder-text: "Description"; }
                }
            }

            HorizontalBox {
                alignment: end;
                spacing: 8px;

                Button {
                    text: "Cancel";
                    clicked => { root.show-form = false; }
                }

                Button {
                    text: "Create";
                    primary: true;
                    clicked => {
                        AppState.create-category(name-input.text, desc-input.text, 0);
                        root.show-form = false;
                    }
                }
            }
        }
    }

    GroupBox {
        title: "Categories";
        vertical-stretch: 1;

        StandardListView {
            model: [
                // Populated from Rust via AppState.categories mapping
            ];
        }
    }
}
