// Generated by Qleany v1.5.5 from feature_use_case.tera
use crate::RecordStockMovementDto;
use crate::RecordStockMovementResultDto;
use anyhow::{Result, anyhow};
use common::database::CommandUnitOfWork;
use common::entities::{Location, Product, Root, StockMovement, User};
use common::types::EntityId;

pub trait RecordStockMovementUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn RecordStockMovementUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update (scalar-only), UpdateMulti (scalar-only),
// UpdateWithRelationships, UpdateWithRelationshipsMulti,
// Remove, RemoveMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/record_stock_movement_uow.rs
//
#[macros::uow_action(entity = "Root", action = "Get")]
#[macros::uow_action(entity = "Root", action = "GetMulti")]
#[macros::uow_action(entity = "Root", action = "Snapshot")]
#[macros::uow_action(entity = "Root", action = "Restore")]
#[macros::uow_action(entity = "User", action = "Get")]
#[macros::uow_action(entity = "User", action = "GetMulti")]
#[macros::uow_action(entity = "User", action = "Snapshot")]
#[macros::uow_action(entity = "User", action = "Restore")]
#[macros::uow_action(entity = "StockMovement", action = "Get")]
#[macros::uow_action(entity = "StockMovement", action = "GetMulti")]
#[macros::uow_action(entity = "StockMovement", action = "Snapshot")]
#[macros::uow_action(entity = "StockMovement", action = "Restore")]
#[macros::uow_action(entity = "Product", action = "Get")]
#[macros::uow_action(entity = "Product", action = "GetMulti")]
#[macros::uow_action(entity = "Product", action = "Snapshot")]
#[macros::uow_action(entity = "Product", action = "Restore")]
#[macros::uow_action(entity = "Location", action = "Get")]
#[macros::uow_action(entity = "Location", action = "GetMulti")]
#[macros::uow_action(entity = "Location", action = "Snapshot")]
#[macros::uow_action(entity = "Location", action = "Restore")]
pub trait RecordStockMovementUnitOfWorkTrait: CommandUnitOfWork {}

pub struct RecordStockMovementUseCase {
    uow_factory: Box<dyn RecordStockMovementUnitOfWorkFactoryTrait>,
}

impl RecordStockMovementUseCase {
    pub fn new(uow_factory: Box<dyn RecordStockMovementUnitOfWorkFactoryTrait>) -> Self {
        RecordStockMovementUseCase { uow_factory }
    }

    pub fn execute(
        &mut self,
        dto: &RecordStockMovementDto,
    ) -> Result<RecordStockMovementResultDto> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: RecordStockMovementUseCase to be implemented
        unimplemented!("RecordStockMovementUseCase unimplemented");
        uow.commit()?;
        //Ok(RecordStockMovementResultDto {
        //
        //})
        // placeholder to allow compilation
        Err(anyhow!("Not implemented"))
    }
}
