// Generated by Qleany v1.5.5 from frontend_entity_commands.tera

//! User entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::user::UserRelationshipField;
use common::types::EntityId;
use direct_access::UserRelationshipDto;
use direct_access::{CreateUserDto, UpdateUserDto, UserDto, user_controller};

/// Create a new user entity (orphan, no parent)
pub fn create_orphan_user(ctx: &AppContext, dto: &CreateUserDto) -> Result<UserDto> {
    user_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto).context("creating user")
}
/// Create a new user entity as child of owner
pub fn create_user(
    ctx: &AppContext,

    dto: &CreateUserDto,
    owner_id: EntityId,
    index: i32,
) -> Result<UserDto> {
    user_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating user")
}
/// Create multiple user entities (orphan, no parent)
pub fn create_orphan_user_multi(ctx: &AppContext, dtos: &[CreateUserDto]) -> Result<Vec<UserDto>> {
    user_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating user entities")
}
/// Create multiple user entities as children of owner
pub fn create_user_multi(
    ctx: &AppContext,

    dtos: &[CreateUserDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<UserDto>> {
    user_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating user entities")
}
/// Get a user entity by ID
pub fn get_user(ctx: &AppContext, id: &EntityId) -> Result<Option<UserDto>> {
    user_controller::get(&ctx.db_context, id).context("getting user")
}

/// Get multiple user entities by IDs
pub fn get_user_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<UserDto>>> {
    user_controller::get_multi(&ctx.db_context, ids).context("getting user entities")
}

/// Get all user entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_user(ctx: &AppContext) -> Result<Vec<UserDto>> {
    user_controller::get_all(&ctx.db_context).context("getting all user entities")
}

/// Update a user entity
pub fn update_user(ctx: &AppContext, dto: &UpdateUserDto) -> Result<UserDto> {
    user_controller::update(&ctx.db_context, &ctx.event_hub, dto).context("updating user")
}

/// Update multiple user entities
pub fn update_user_multi(ctx: &AppContext, dtos: &[UpdateUserDto]) -> Result<Vec<UserDto>> {
    user_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating user entities")
}

/// Update a user entity with relationships
pub fn update_user_with_relationships(ctx: &AppContext, dto: &UserDto) -> Result<UserDto> {
    user_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating user with relationships")
}

/// Update multiple user entities with relationships
pub fn update_user_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[UserDto],
) -> Result<Vec<UserDto>> {
    user_controller::update_with_relationships_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating user entities with relationships")
}

/// Remove a user entity by ID
pub fn remove_user(ctx: &AppContext, id: &EntityId) -> Result<()> {
    user_controller::remove(&ctx.db_context, &ctx.event_hub, id).context("removing user")
}

/// Remove multiple user entities by IDs
pub fn remove_user_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    user_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing user entities")
}

/// Get a user relationship
pub fn get_user_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &UserRelationshipField,
) -> Result<Vec<EntityId>> {
    user_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting user relationship")
}

/// Get relationship IDs for multiple user entities at once
pub fn get_user_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &UserRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    user_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting user relationships (many)")
}

/// Get relationship count for a user entity
pub fn get_user_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &UserRelationshipField,
) -> Result<usize> {
    user_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting user relationship count")
}

/// Get relationship IDs for a user entity with pagination
pub fn get_user_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &UserRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    user_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting user relationship range")
}

/// Set a user relationship
pub fn set_user_relationship(ctx: &AppContext, dto: &UserRelationshipDto) -> Result<()> {
    user_controller::set_relationship(&ctx.db_context, &ctx.event_hub, dto)
        .context("setting user relationship")
}

/// Move (reorder) IDs within a user relationship
pub fn move_user_relationship(
    ctx: &AppContext,

    id: &EntityId,
    field: &UserRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    user_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving user relationship")
}
