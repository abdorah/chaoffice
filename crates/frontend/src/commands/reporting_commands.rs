// Generated by Qleany v1.5.5 from frontend_feature_commands.tera

//! Reporting feature commands

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use reporting::{
    GenerateBudgetReportDto, GenerateBudgetReportReturnDto, GenerateInventoryReportDto,
    GeneratePurchasingReportDto, GeneratePurchasingReportReturnDto, GenerateReportReturnDto,
    GenerateStockMovementReportDto, GenerateStockReportReturnDto, reporting_controller,
};

use common::long_operation::OperationProgress;

/// generate_inventory_report (long operation)
pub fn generate_inventory_report(
    ctx: &AppContext,
    dto: &GenerateInventoryReportDto,
) -> Result<String> {
    reporting_controller::generate_inventory_report(
        &ctx.db_context,
        &ctx.event_hub,
        &mut ctx.long_operation_manager.lock().unwrap(),
        dto,
    )
    .context("generate_inventory_report")
}

/// Get the progress of a generate_inventory_report operation
pub fn get_generate_inventory_report_progress(
    ctx: &AppContext,
    operation_id: &str,
) -> Option<OperationProgress> {
    reporting_controller::get_generate_inventory_report_progress(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
}

/// Get the result of a generate_inventory_report operation
pub fn get_generate_inventory_report_result(
    ctx: &AppContext,
    operation_id: &str,
) -> Result<Option<GenerateReportReturnDto>> {
    reporting_controller::get_generate_inventory_report_result(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
    .context("getting generate_inventory_report result")
}

/// generate_stock_movement_report (long operation)
pub fn generate_stock_movement_report(
    ctx: &AppContext,
    dto: &GenerateStockMovementReportDto,
) -> Result<String> {
    reporting_controller::generate_stock_movement_report(
        &ctx.db_context,
        &ctx.event_hub,
        &mut ctx.long_operation_manager.lock().unwrap(),
        dto,
    )
    .context("generate_stock_movement_report")
}

/// Get the progress of a generate_stock_movement_report operation
pub fn get_generate_stock_movement_report_progress(
    ctx: &AppContext,
    operation_id: &str,
) -> Option<OperationProgress> {
    reporting_controller::get_generate_stock_movement_report_progress(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
}

/// Get the result of a generate_stock_movement_report operation
pub fn get_generate_stock_movement_report_result(
    ctx: &AppContext,
    operation_id: &str,
) -> Result<Option<GenerateStockReportReturnDto>> {
    reporting_controller::get_generate_stock_movement_report_result(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
    .context("getting generate_stock_movement_report result")
}

/// generate_budget_report (long operation)
pub fn generate_budget_report(ctx: &AppContext, dto: &GenerateBudgetReportDto) -> Result<String> {
    reporting_controller::generate_budget_report(
        &ctx.db_context,
        &ctx.event_hub,
        &mut ctx.long_operation_manager.lock().unwrap(),
        dto,
    )
    .context("generate_budget_report")
}

/// Get the progress of a generate_budget_report operation
pub fn get_generate_budget_report_progress(
    ctx: &AppContext,
    operation_id: &str,
) -> Option<OperationProgress> {
    reporting_controller::get_generate_budget_report_progress(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
}

/// Get the result of a generate_budget_report operation
pub fn get_generate_budget_report_result(
    ctx: &AppContext,
    operation_id: &str,
) -> Result<Option<GenerateBudgetReportReturnDto>> {
    reporting_controller::get_generate_budget_report_result(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
    .context("getting generate_budget_report result")
}

/// generate_purchasing_report (long operation)
pub fn generate_purchasing_report(
    ctx: &AppContext,
    dto: &GeneratePurchasingReportDto,
) -> Result<String> {
    reporting_controller::generate_purchasing_report(
        &ctx.db_context,
        &ctx.event_hub,
        &mut ctx.long_operation_manager.lock().unwrap(),
        dto,
    )
    .context("generate_purchasing_report")
}

/// Get the progress of a generate_purchasing_report operation
pub fn get_generate_purchasing_report_progress(
    ctx: &AppContext,
    operation_id: &str,
) -> Option<OperationProgress> {
    reporting_controller::get_generate_purchasing_report_progress(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
}

/// Get the result of a generate_purchasing_report operation
pub fn get_generate_purchasing_report_result(
    ctx: &AppContext,
    operation_id: &str,
) -> Result<Option<GeneratePurchasingReportReturnDto>> {
    reporting_controller::get_generate_purchasing_report_result(
        &ctx.long_operation_manager.lock().unwrap(),
        operation_id,
    )
    .context("getting generate_purchasing_report result")
}
