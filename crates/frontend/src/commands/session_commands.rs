// Generated by Qleany v1.5.5 from frontend_entity_commands.tera

//! Session entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{CreateSessionDto, SessionDto, UpdateSessionDto, session_controller};

/// Create a new session entity (orphan, no parent)
pub fn create_orphan_session(ctx: &AppContext, dto: &CreateSessionDto) -> Result<SessionDto> {
    session_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto)
        .context("creating session")
}
/// Create a new session entity as child of owner
pub fn create_session(
    ctx: &AppContext,

    dto: &CreateSessionDto,
    owner_id: EntityId,
    index: i32,
) -> Result<SessionDto> {
    session_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating session")
}
/// Create multiple session entities (orphan, no parent)
pub fn create_orphan_session_multi(
    ctx: &AppContext,

    dtos: &[CreateSessionDto],
) -> Result<Vec<SessionDto>> {
    session_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating session entities")
}
/// Create multiple session entities as children of owner
pub fn create_session_multi(
    ctx: &AppContext,

    dtos: &[CreateSessionDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<SessionDto>> {
    session_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating session entities")
}
/// Get a session entity by ID
pub fn get_session(ctx: &AppContext, id: &EntityId) -> Result<Option<SessionDto>> {
    session_controller::get(&ctx.db_context, id).context("getting session")
}

/// Get multiple session entities by IDs
pub fn get_session_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<SessionDto>>> {
    session_controller::get_multi(&ctx.db_context, ids).context("getting session entities")
}

/// Get all session entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_session(ctx: &AppContext) -> Result<Vec<SessionDto>> {
    session_controller::get_all(&ctx.db_context).context("getting all session entities")
}

/// Update a session entity
pub fn update_session(ctx: &AppContext, dto: &UpdateSessionDto) -> Result<SessionDto> {
    session_controller::update(&ctx.db_context, &ctx.event_hub, dto).context("updating session")
}

/// Update multiple session entities
pub fn update_session_multi(
    ctx: &AppContext,

    dtos: &[UpdateSessionDto],
) -> Result<Vec<SessionDto>> {
    session_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating session entities")
}

/// Update a session entity with relationships
pub fn update_session_with_relationships(ctx: &AppContext, dto: &SessionDto) -> Result<SessionDto> {
    session_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating session with relationships")
}

/// Update multiple session entities with relationships
pub fn update_session_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[SessionDto],
) -> Result<Vec<SessionDto>> {
    session_controller::update_with_relationships_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating session entities with relationships")
}

/// Remove a session entity by ID
pub fn remove_session(ctx: &AppContext, id: &EntityId) -> Result<()> {
    session_controller::remove(&ctx.db_context, &ctx.event_hub, id).context("removing session")
}

/// Remove multiple session entities by IDs
pub fn remove_session_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    session_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing session entities")
}
