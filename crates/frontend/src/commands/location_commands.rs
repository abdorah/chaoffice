// Generated by Qleany v1.5.5 from frontend_entity_commands.tera

//! Location entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{CreateLocationDto, LocationDto, UpdateLocationDto, location_controller};

/// Create a new location entity (orphan, no parent)
pub fn create_orphan_location(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateLocationDto,
) -> Result<LocationDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating location")
}
/// Create a new location entity as child of owner
pub fn create_location(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateLocationDto,
    owner_id: EntityId,
    index: i32,
) -> Result<LocationDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating location")
}
/// Create multiple location entities (orphan, no parent)
pub fn create_orphan_location_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateLocationDto],
) -> Result<Vec<LocationDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating location entities")
}
/// Create multiple location entities as children of owner
pub fn create_location_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateLocationDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<LocationDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating location entities")
}
/// Get a location entity by ID
pub fn get_location(ctx: &AppContext, id: &EntityId) -> Result<Option<LocationDto>> {
    location_controller::get(&ctx.db_context, id).context("getting location")
}

/// Get multiple location entities by IDs
pub fn get_location_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<LocationDto>>> {
    location_controller::get_multi(&ctx.db_context, ids).context("getting location entities")
}

/// Get all location entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_location(ctx: &AppContext) -> Result<Vec<LocationDto>> {
    location_controller::get_all(&ctx.db_context).context("getting all location entities")
}

/// Update a location entity
pub fn update_location(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateLocationDto,
) -> Result<LocationDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating location")
}

/// Update multiple location entities
pub fn update_location_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateLocationDto],
) -> Result<Vec<LocationDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating location entities")
}

/// Update a location entity with relationships
pub fn update_location_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &LocationDto,
) -> Result<LocationDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating location with relationships")
}

/// Update multiple location entities with relationships
pub fn update_location_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[LocationDto],
) -> Result<Vec<LocationDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating location entities with relationships")
}

/// Remove a location entity by ID
pub fn remove_location(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing location")
}

/// Remove multiple location entities by IDs
pub fn remove_location_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    location_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing location entities")
}
