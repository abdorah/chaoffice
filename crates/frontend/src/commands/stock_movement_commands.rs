// Generated by Qleany v1.5.5 from frontend_entity_commands.tera

//! StockMovement entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::stock_movement::StockMovementRelationshipField;
use common::types::EntityId;
use direct_access::StockMovementRelationshipDto;
use direct_access::{
    CreateStockMovementDto, StockMovementDto, UpdateStockMovementDto, stock_movement_controller,
};

/// Create a new stock_movement entity (orphan, no parent)
pub fn create_orphan_stock_movement(
    ctx: &AppContext,

    dto: &CreateStockMovementDto,
) -> Result<StockMovementDto> {
    stock_movement_controller::create_orphan(&ctx.db_context, &ctx.event_hub, dto)
        .context("creating stock_movement")
}
/// Create a new stock_movement entity as child of owner
pub fn create_stock_movement(
    ctx: &AppContext,

    dto: &CreateStockMovementDto,
    owner_id: EntityId,
    index: i32,
) -> Result<StockMovementDto> {
    stock_movement_controller::create(&ctx.db_context, &ctx.event_hub, dto, owner_id, index)
        .context("creating stock_movement")
}
/// Create multiple stock_movement entities (orphan, no parent)
pub fn create_orphan_stock_movement_multi(
    ctx: &AppContext,

    dtos: &[CreateStockMovementDto],
) -> Result<Vec<StockMovementDto>> {
    stock_movement_controller::create_orphan_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("creating stock_movement entities")
}
/// Create multiple stock_movement entities as children of owner
pub fn create_stock_movement_multi(
    ctx: &AppContext,

    dtos: &[CreateStockMovementDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<StockMovementDto>> {
    stock_movement_controller::create_multi(&ctx.db_context, &ctx.event_hub, dtos, owner_id, index)
        .context("creating stock_movement entities")
}
/// Get a stock_movement entity by ID
pub fn get_stock_movement(ctx: &AppContext, id: &EntityId) -> Result<Option<StockMovementDto>> {
    stock_movement_controller::get(&ctx.db_context, id).context("getting stock_movement")
}

/// Get multiple stock_movement entities by IDs
pub fn get_stock_movement_multi(
    ctx: &AppContext,
    ids: &[EntityId],
) -> Result<Vec<Option<StockMovementDto>>> {
    stock_movement_controller::get_multi(&ctx.db_context, ids)
        .context("getting stock_movement entities")
}

/// Get all stock_movement entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_stock_movement(ctx: &AppContext) -> Result<Vec<StockMovementDto>> {
    stock_movement_controller::get_all(&ctx.db_context)
        .context("getting all stock_movement entities")
}

/// Update a stock_movement entity
pub fn update_stock_movement(
    ctx: &AppContext,

    dto: &UpdateStockMovementDto,
) -> Result<StockMovementDto> {
    stock_movement_controller::update(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating stock_movement")
}

/// Update multiple stock_movement entities
pub fn update_stock_movement_multi(
    ctx: &AppContext,

    dtos: &[UpdateStockMovementDto],
) -> Result<Vec<StockMovementDto>> {
    stock_movement_controller::update_multi(&ctx.db_context, &ctx.event_hub, dtos)
        .context("updating stock_movement entities")
}

/// Update a stock_movement entity with relationships
pub fn update_stock_movement_with_relationships(
    ctx: &AppContext,

    dto: &StockMovementDto,
) -> Result<StockMovementDto> {
    stock_movement_controller::update_with_relationships(&ctx.db_context, &ctx.event_hub, dto)
        .context("updating stock_movement with relationships")
}

/// Update multiple stock_movement entities with relationships
pub fn update_stock_movement_with_relationships_multi(
    ctx: &AppContext,

    dtos: &[StockMovementDto],
) -> Result<Vec<StockMovementDto>> {
    stock_movement_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        dtos,
    )
    .context("updating stock_movement entities with relationships")
}

/// Remove a stock_movement entity by ID
pub fn remove_stock_movement(ctx: &AppContext, id: &EntityId) -> Result<()> {
    stock_movement_controller::remove(&ctx.db_context, &ctx.event_hub, id)
        .context("removing stock_movement")
}

/// Remove multiple stock_movement entities by IDs
pub fn remove_stock_movement_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<()> {
    stock_movement_controller::remove_multi(&ctx.db_context, &ctx.event_hub, ids)
        .context("removing stock_movement entities")
}

/// Get a stock_movement relationship
pub fn get_stock_movement_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &StockMovementRelationshipField,
) -> Result<Vec<EntityId>> {
    stock_movement_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting stock_movement relationship")
}

/// Get relationship IDs for multiple stock_movement entities at once
pub fn get_stock_movement_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &StockMovementRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    stock_movement_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting stock_movement relationships (many)")
}

/// Get relationship count for a stock_movement entity
pub fn get_stock_movement_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &StockMovementRelationshipField,
) -> Result<usize> {
    stock_movement_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting stock_movement relationship count")
}

/// Get relationship IDs for a stock_movement entity with pagination
pub fn get_stock_movement_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &StockMovementRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    stock_movement_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting stock_movement relationship range")
}

/// Set a stock_movement relationship
pub fn set_stock_movement_relationship(
    ctx: &AppContext,

    dto: &StockMovementRelationshipDto,
) -> Result<()> {
    stock_movement_controller::set_relationship(&ctx.db_context, &ctx.event_hub, dto)
        .context("setting stock_movement relationship")
}

/// Move (reorder) IDs within a stock_movement relationship
pub fn move_stock_movement_relationship(
    ctx: &AppContext,

    id: &EntityId,
    field: &StockMovementRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    stock_movement_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving stock_movement relationship")
}
