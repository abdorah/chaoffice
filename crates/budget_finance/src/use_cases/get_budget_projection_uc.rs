// Generated by Qleany v1.5.5 from feature_use_case.tera
use crate::BudgetProjectionDto;
use crate::GetBudgetProjectionDto;
use anyhow::{Result, anyhow};
use common::database::QueryUnitOfWork;
use common::entities::{BudgetEntry, Deal, Product};
use common::types::EntityId;

pub trait GetBudgetProjectionUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn GetBudgetProjectionUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// GetRO, GetMultiRO, GetRelationship, GetRelationshipRO,
// GetRelationshipsFromRightIdsRO
//
// You have here a read-only unit of work trait.
//
// RO means Read Only, so *RO actions should be used here.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/get_budget_projection_uow.rs
//
#[macros::uow_action(entity = "BudgetEntry", action = "GetRO")]
#[macros::uow_action(entity = "BudgetEntry", action = "GetMultiRO")]
#[macros::uow_action(entity = "Product", action = "GetRO")]
#[macros::uow_action(entity = "Product", action = "GetMultiRO")]
#[macros::uow_action(entity = "Deal", action = "GetRO")]
#[macros::uow_action(entity = "Deal", action = "GetMultiRO")]
pub trait GetBudgetProjectionUnitOfWorkTrait: QueryUnitOfWork {}

pub struct GetBudgetProjectionUseCase {
    uow_factory: Box<dyn GetBudgetProjectionUnitOfWorkFactoryTrait>,
}

impl GetBudgetProjectionUseCase {
    pub fn new(uow_factory: Box<dyn GetBudgetProjectionUnitOfWorkFactoryTrait>) -> Self {
        GetBudgetProjectionUseCase { uow_factory }
    }

    pub fn execute(&mut self, dto: &GetBudgetProjectionDto) -> Result<BudgetProjectionDto> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: GetBudgetProjectionUseCase to be implemented
        unimplemented!("GetBudgetProjectionUseCase unimplemented");
        uow.end_transaction()?;
        //Ok(BudgetProjectionDto {
        //
        //})
        // placeholder to allow compilation
        Err(anyhow!("Not implemented"))
    }
}
