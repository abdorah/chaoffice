// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::Root;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct RootDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub products: Vec<EntityId>,
    pub categories: Vec<EntityId>,
    pub persons: Vec<EntityId>,
    pub locations: Vec<EntityId>,
    pub deals: Vec<EntityId>,
    pub users: Vec<EntityId>,
    pub stock_movements: Vec<EntityId>,
    pub budget_entries: Vec<EntityId>,
}

impl From<RootDto> for Root {
    fn from(dto: RootDto) -> Self {
        Root {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            products: dto.products,
            categories: dto.categories,
            persons: dto.persons,
            locations: dto.locations,
            deals: dto.deals,
            users: dto.users,
            stock_movements: dto.stock_movements,
            budget_entries: dto.budget_entries,
        }
    }
}

impl From<&RootDto> for Root {
    fn from(dto: &RootDto) -> Self {
        Root {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            products: dto.products.clone(),
            categories: dto.categories.clone(),
            persons: dto.persons.clone(),
            locations: dto.locations.clone(),
            deals: dto.deals.clone(),
            users: dto.users.clone(),
            stock_movements: dto.stock_movements.clone(),
            budget_entries: dto.budget_entries.clone(),
        }
    }
}

impl From<Root> for RootDto {
    fn from(entity: Root) -> Self {
        RootDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            products: entity.products,
            categories: entity.categories,
            persons: entity.persons,
            locations: entity.locations,
            deals: entity.deals,
            users: entity.users,
            stock_movements: entity.stock_movements,
            budget_entries: entity.budget_entries,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateRootDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub products: Vec<EntityId>,
    pub categories: Vec<EntityId>,
    pub persons: Vec<EntityId>,
    pub locations: Vec<EntityId>,
    pub deals: Vec<EntityId>,
    pub users: Vec<EntityId>,
    pub stock_movements: Vec<EntityId>,
    pub budget_entries: Vec<EntityId>,
}

impl From<CreateRootDto> for Root {
    fn from(dto: CreateRootDto) -> Self {
        Root {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            products: dto.products,
            categories: dto.categories,
            persons: dto.persons,
            locations: dto.locations,
            deals: dto.deals,
            users: dto.users,
            stock_movements: dto.stock_movements,
            budget_entries: dto.budget_entries,
        }
    }
}

impl From<&CreateRootDto> for Root {
    fn from(dto: &CreateRootDto) -> Self {
        Root {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            products: dto.products.clone(),
            categories: dto.categories.clone(),
            persons: dto.persons.clone(),
            locations: dto.locations.clone(),
            deals: dto.deals.clone(),
            users: dto.users.clone(),
            stock_movements: dto.stock_movements.clone(),
            budget_entries: dto.budget_entries.clone(),
        }
    }
}

impl From<Root> for CreateRootDto {
    fn from(entity: Root) -> Self {
        CreateRootDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            products: entity.products,
            categories: entity.categories,
            persons: entity.persons,
            locations: entity.locations,
            deals: entity.deals,
            users: entity.users,
            stock_movements: entity.stock_movements,
            budget_entries: entity.budget_entries,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateRootDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
}

impl From<UpdateRootDto> for Root {
    fn from(dto: UpdateRootDto) -> Self {
        Root {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            products: Default::default(),
            categories: Default::default(),
            persons: Default::default(),
            locations: Default::default(),
            deals: Default::default(),
            users: Default::default(),
            stock_movements: Default::default(),
            budget_entries: Default::default(),
        }
    }
}

impl From<&UpdateRootDto> for Root {
    fn from(dto: &UpdateRootDto) -> Self {
        Root {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            products: Default::default(),
            categories: Default::default(),
            persons: Default::default(),
            locations: Default::default(),
            deals: Default::default(),
            users: Default::default(),
            stock_movements: Default::default(),
            budget_entries: Default::default(),
        }
    }
}

impl From<Root> for UpdateRootDto {
    fn from(entity: Root) -> Self {
        UpdateRootDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
        }
    }
}

impl From<RootDto> for UpdateRootDto {
    fn from(dto: RootDto) -> Self {
        UpdateRootDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
        }
    }
}
pub use common::direct_access::root::RootRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct RootRelationshipDto {
    pub id: EntityId,
    pub field: RootRelationshipField,
    pub right_ids: Vec<EntityId>,
}
