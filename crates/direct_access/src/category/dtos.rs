// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::Category;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CategoryDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub description: String,
    pub parent_category: Option<EntityId>,
    pub subcategories: Vec<EntityId>,
}

impl From<CategoryDto> for Category {
    fn from(dto: CategoryDto) -> Self {
        Category {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            description: dto.description,
            parent_category: dto.parent_category,
            subcategories: dto.subcategories,
        }
    }
}

impl From<&CategoryDto> for Category {
    fn from(dto: &CategoryDto) -> Self {
        Category {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            description: dto.description.clone(),
            parent_category: dto.parent_category.clone(),
            subcategories: dto.subcategories.clone(),
        }
    }
}

impl From<Category> for CategoryDto {
    fn from(entity: Category) -> Self {
        CategoryDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            description: entity.description,
            parent_category: entity.parent_category,
            subcategories: entity.subcategories,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateCategoryDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub description: String,
    pub parent_category: Option<EntityId>,
    pub subcategories: Vec<EntityId>,
}

impl From<CreateCategoryDto> for Category {
    fn from(dto: CreateCategoryDto) -> Self {
        Category {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            description: dto.description,
            parent_category: dto.parent_category,
            subcategories: dto.subcategories,
        }
    }
}

impl From<&CreateCategoryDto> for Category {
    fn from(dto: &CreateCategoryDto) -> Self {
        Category {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            description: dto.description.clone(),
            parent_category: dto.parent_category.clone(),
            subcategories: dto.subcategories.clone(),
        }
    }
}

impl From<Category> for CreateCategoryDto {
    fn from(entity: Category) -> Self {
        CreateCategoryDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            description: entity.description,
            parent_category: entity.parent_category,
            subcategories: entity.subcategories,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateCategoryDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub description: String,
}

impl From<UpdateCategoryDto> for Category {
    fn from(dto: UpdateCategoryDto) -> Self {
        Category {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            description: dto.description,
            parent_category: Default::default(),
            subcategories: Default::default(),
        }
    }
}

impl From<&UpdateCategoryDto> for Category {
    fn from(dto: &UpdateCategoryDto) -> Self {
        Category {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            description: dto.description.clone(),
            parent_category: Default::default(),
            subcategories: Default::default(),
        }
    }
}

impl From<Category> for UpdateCategoryDto {
    fn from(entity: Category) -> Self {
        UpdateCategoryDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            description: entity.description,
        }
    }
}

impl From<CategoryDto> for UpdateCategoryDto {
    fn from(dto: CategoryDto) -> Self {
        UpdateCategoryDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            description: dto.description,
        }
    }
}
pub use common::direct_access::category::CategoryRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CategoryRelationshipDto {
    pub id: EntityId,
    pub field: CategoryRelationshipField,
    pub right_ids: Vec<EntityId>,
}
