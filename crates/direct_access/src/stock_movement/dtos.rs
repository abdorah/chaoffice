// Generated by Qleany v1.5.5 from entity_dtos.tera

pub use common::entities::MovementType;
use common::entities::StockMovement;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct StockMovementDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub movement_type: MovementType,
    pub quantity: i64,
    pub note: String,
    pub product: Option<EntityId>,
    pub from_location: Option<EntityId>,
    pub to_location: Option<EntityId>,
    pub performed_by: Option<EntityId>,
}

impl From<StockMovementDto> for StockMovement {
    fn from(dto: StockMovementDto) -> Self {
        StockMovement {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            movement_type: dto.movement_type,
            quantity: dto.quantity,
            note: dto.note,
            product: dto.product,
            from_location: dto.from_location,
            to_location: dto.to_location,
            performed_by: dto.performed_by,
        }
    }
}

impl From<&StockMovementDto> for StockMovement {
    fn from(dto: &StockMovementDto) -> Self {
        StockMovement {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            movement_type: dto.movement_type.clone(),
            quantity: dto.quantity.clone(),
            note: dto.note.clone(),
            product: dto.product.clone(),
            from_location: dto.from_location.clone(),
            to_location: dto.to_location.clone(),
            performed_by: dto.performed_by.clone(),
        }
    }
}

impl From<StockMovement> for StockMovementDto {
    fn from(entity: StockMovement) -> Self {
        StockMovementDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            movement_type: entity.movement_type,
            quantity: entity.quantity,
            note: entity.note,
            product: entity.product,
            from_location: entity.from_location,
            to_location: entity.to_location,
            performed_by: entity.performed_by,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateStockMovementDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub movement_type: MovementType,
    pub quantity: i64,
    pub note: String,
    pub product: Option<EntityId>,
    pub from_location: Option<EntityId>,
    pub to_location: Option<EntityId>,
    pub performed_by: Option<EntityId>,
}

impl From<CreateStockMovementDto> for StockMovement {
    fn from(dto: CreateStockMovementDto) -> Self {
        StockMovement {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            movement_type: dto.movement_type,
            quantity: dto.quantity,
            note: dto.note,
            product: dto.product,
            from_location: dto.from_location,
            to_location: dto.to_location,
            performed_by: dto.performed_by,
        }
    }
}

impl From<&CreateStockMovementDto> for StockMovement {
    fn from(dto: &CreateStockMovementDto) -> Self {
        StockMovement {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            movement_type: dto.movement_type.clone(),
            quantity: dto.quantity.clone(),
            note: dto.note.clone(),
            product: dto.product.clone(),
            from_location: dto.from_location.clone(),
            to_location: dto.to_location.clone(),
            performed_by: dto.performed_by.clone(),
        }
    }
}

impl From<StockMovement> for CreateStockMovementDto {
    fn from(entity: StockMovement) -> Self {
        CreateStockMovementDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            movement_type: entity.movement_type,
            quantity: entity.quantity,
            note: entity.note,
            product: entity.product,
            from_location: entity.from_location,
            to_location: entity.to_location,
            performed_by: entity.performed_by,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateStockMovementDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub movement_type: MovementType,
    pub quantity: i64,
    pub note: String,
}

impl From<UpdateStockMovementDto> for StockMovement {
    fn from(dto: UpdateStockMovementDto) -> Self {
        StockMovement {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            movement_type: dto.movement_type,
            quantity: dto.quantity,
            note: dto.note,
            product: Default::default(),
            from_location: Default::default(),
            to_location: Default::default(),
            performed_by: Default::default(),
        }
    }
}

impl From<&UpdateStockMovementDto> for StockMovement {
    fn from(dto: &UpdateStockMovementDto) -> Self {
        StockMovement {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            movement_type: dto.movement_type.clone(),
            quantity: dto.quantity.clone(),
            note: dto.note.clone(),
            product: Default::default(),
            from_location: Default::default(),
            to_location: Default::default(),
            performed_by: Default::default(),
        }
    }
}

impl From<StockMovement> for UpdateStockMovementDto {
    fn from(entity: StockMovement) -> Self {
        UpdateStockMovementDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            movement_type: entity.movement_type,
            quantity: entity.quantity,
            note: entity.note,
        }
    }
}

impl From<StockMovementDto> for UpdateStockMovementDto {
    fn from(dto: StockMovementDto) -> Self {
        UpdateStockMovementDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            movement_type: dto.movement_type,
            quantity: dto.quantity,
            note: dto.note,
        }
    }
}
pub use common::direct_access::stock_movement::StockMovementRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct StockMovementRelationshipDto {
    pub id: EntityId,
    pub field: StockMovementRelationshipField,
    pub right_ids: Vec<EntityId>,
}
