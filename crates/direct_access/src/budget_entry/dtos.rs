// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::BudgetEntry;
pub use common::entities::BudgetEntryType;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct BudgetEntryDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub entry_type: BudgetEntryType,
    pub amount: f64,
    pub description: String,
    pub entry_date: chrono::DateTime<chrono::Utc>,
    pub product: Option<EntityId>,
    pub deal: Option<EntityId>,
    pub recorded_by: Option<EntityId>,
}

impl From<BudgetEntryDto> for BudgetEntry {
    fn from(dto: BudgetEntryDto) -> Self {
        BudgetEntry {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            entry_type: dto.entry_type,
            amount: dto.amount,
            description: dto.description,
            entry_date: dto.entry_date,
            product: dto.product,
            deal: dto.deal,
            recorded_by: dto.recorded_by,
        }
    }
}

impl From<&BudgetEntryDto> for BudgetEntry {
    fn from(dto: &BudgetEntryDto) -> Self {
        BudgetEntry {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            entry_type: dto.entry_type.clone(),
            amount: dto.amount.clone(),
            description: dto.description.clone(),
            entry_date: dto.entry_date.clone(),
            product: dto.product.clone(),
            deal: dto.deal.clone(),
            recorded_by: dto.recorded_by.clone(),
        }
    }
}

impl From<BudgetEntry> for BudgetEntryDto {
    fn from(entity: BudgetEntry) -> Self {
        BudgetEntryDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            entry_type: entity.entry_type,
            amount: entity.amount,
            description: entity.description,
            entry_date: entity.entry_date,
            product: entity.product,
            deal: entity.deal,
            recorded_by: entity.recorded_by,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateBudgetEntryDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub entry_type: BudgetEntryType,
    pub amount: f64,
    pub description: String,
    pub entry_date: chrono::DateTime<chrono::Utc>,
    pub product: Option<EntityId>,
    pub deal: Option<EntityId>,
    pub recorded_by: Option<EntityId>,
}

impl From<CreateBudgetEntryDto> for BudgetEntry {
    fn from(dto: CreateBudgetEntryDto) -> Self {
        BudgetEntry {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            entry_type: dto.entry_type,
            amount: dto.amount,
            description: dto.description,
            entry_date: dto.entry_date,
            product: dto.product,
            deal: dto.deal,
            recorded_by: dto.recorded_by,
        }
    }
}

impl From<&CreateBudgetEntryDto> for BudgetEntry {
    fn from(dto: &CreateBudgetEntryDto) -> Self {
        BudgetEntry {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            entry_type: dto.entry_type.clone(),
            amount: dto.amount.clone(),
            description: dto.description.clone(),
            entry_date: dto.entry_date.clone(),
            product: dto.product.clone(),
            deal: dto.deal.clone(),
            recorded_by: dto.recorded_by.clone(),
        }
    }
}

impl From<BudgetEntry> for CreateBudgetEntryDto {
    fn from(entity: BudgetEntry) -> Self {
        CreateBudgetEntryDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            entry_type: entity.entry_type,
            amount: entity.amount,
            description: entity.description,
            entry_date: entity.entry_date,
            product: entity.product,
            deal: entity.deal,
            recorded_by: entity.recorded_by,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateBudgetEntryDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub entry_type: BudgetEntryType,
    pub amount: f64,
    pub description: String,
    pub entry_date: chrono::DateTime<chrono::Utc>,
}

impl From<UpdateBudgetEntryDto> for BudgetEntry {
    fn from(dto: UpdateBudgetEntryDto) -> Self {
        BudgetEntry {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            entry_type: dto.entry_type,
            amount: dto.amount,
            description: dto.description,
            entry_date: dto.entry_date,
            product: Default::default(),
            deal: Default::default(),
            recorded_by: Default::default(),
        }
    }
}

impl From<&UpdateBudgetEntryDto> for BudgetEntry {
    fn from(dto: &UpdateBudgetEntryDto) -> Self {
        BudgetEntry {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            entry_type: dto.entry_type.clone(),
            amount: dto.amount.clone(),
            description: dto.description.clone(),
            entry_date: dto.entry_date.clone(),
            product: Default::default(),
            deal: Default::default(),
            recorded_by: Default::default(),
        }
    }
}

impl From<BudgetEntry> for UpdateBudgetEntryDto {
    fn from(entity: BudgetEntry) -> Self {
        UpdateBudgetEntryDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            entry_type: entity.entry_type,
            amount: entity.amount,
            description: entity.description,
            entry_date: entity.entry_date,
        }
    }
}

impl From<BudgetEntryDto> for UpdateBudgetEntryDto {
    fn from(dto: BudgetEntryDto) -> Self {
        UpdateBudgetEntryDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            entry_type: dto.entry_type,
            amount: dto.amount,
            description: dto.description,
            entry_date: dto.entry_date,
        }
    }
}
pub use common::direct_access::budget_entry::BudgetEntryRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct BudgetEntryRelationshipDto {
    pub id: EntityId,
    pub field: BudgetEntryRelationshipField,
    pub right_ids: Vec<EntityId>,
}
