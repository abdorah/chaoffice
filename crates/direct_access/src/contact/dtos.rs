// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::Contact;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct ContactDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub phone: String,
    pub email: String,
}

impl From<ContactDto> for Contact {
    fn from(dto: ContactDto) -> Self {
        Contact {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            phone: dto.phone,
            email: dto.email,
        }
    }
}

impl From<&ContactDto> for Contact {
    fn from(dto: &ContactDto) -> Self {
        Contact {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            phone: dto.phone.clone(),
            email: dto.email.clone(),
        }
    }
}

impl From<Contact> for ContactDto {
    fn from(entity: Contact) -> Self {
        ContactDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            phone: entity.phone,
            email: entity.email,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateContactDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub phone: String,
    pub email: String,
}

impl From<CreateContactDto> for Contact {
    fn from(dto: CreateContactDto) -> Self {
        Contact {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            phone: dto.phone,
            email: dto.email,
        }
    }
}

impl From<&CreateContactDto> for Contact {
    fn from(dto: &CreateContactDto) -> Self {
        Contact {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            phone: dto.phone.clone(),
            email: dto.email.clone(),
        }
    }
}

impl From<Contact> for CreateContactDto {
    fn from(entity: Contact) -> Self {
        CreateContactDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            phone: entity.phone,
            email: entity.email,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateContactDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub phone: String,
    pub email: String,
}

impl From<UpdateContactDto> for Contact {
    fn from(dto: UpdateContactDto) -> Self {
        Contact {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            phone: dto.phone,
            email: dto.email,
        }
    }
}

impl From<&UpdateContactDto> for Contact {
    fn from(dto: &UpdateContactDto) -> Self {
        Contact {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            phone: dto.phone.clone(),
            email: dto.email.clone(),
        }
    }
}

impl From<Contact> for UpdateContactDto {
    fn from(entity: Contact) -> Self {
        UpdateContactDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            phone: entity.phone,
            email: entity.email,
        }
    }
}

impl From<ContactDto> for UpdateContactDto {
    fn from(dto: ContactDto) -> Self {
        UpdateContactDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            phone: dto.phone,
            email: dto.email,
        }
    }
}
