// Generated by Qleany v1.5.5 from entity_dtos.tera
// Modified: added manager field

use common::entities::Location;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct LocationDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub address: String,
    pub latitude: f64,
    pub longitude: f64,
    pub capacity: i64,
    pub manager: Option<EntityId>,
}

impl From<LocationDto> for Location {
    fn from(dto: LocationDto) -> Self {
        Location { id: dto.id, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name, address: dto.address, latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<&LocationDto> for Location {
    fn from(dto: &LocationDto) -> Self {
        Location { id: dto.id, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name.clone(), address: dto.address.clone(), latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<Location> for LocationDto {
    fn from(e: Location) -> Self {
        LocationDto { id: e.id, created_at: e.created_at, updated_at: e.updated_at, name: e.name, address: e.address, latitude: e.latitude, longitude: e.longitude, capacity: e.capacity, manager: e.manager }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateLocationDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub address: String,
    pub latitude: f64,
    pub longitude: f64,
    pub capacity: i64,
    pub manager: Option<EntityId>,
}

impl From<CreateLocationDto> for Location {
    fn from(dto: CreateLocationDto) -> Self {
        Location { id: 0, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name, address: dto.address, latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<&CreateLocationDto> for Location {
    fn from(dto: &CreateLocationDto) -> Self {
        Location { id: 0, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name.clone(), address: dto.address.clone(), latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<Location> for CreateLocationDto {
    fn from(e: Location) -> Self {
        CreateLocationDto { created_at: e.created_at, updated_at: e.updated_at, name: e.name, address: e.address, latitude: e.latitude, longitude: e.longitude, capacity: e.capacity, manager: e.manager }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateLocationDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub address: String,
    pub latitude: f64,
    pub longitude: f64,
    pub capacity: i64,
    pub manager: Option<EntityId>,
}

impl From<UpdateLocationDto> for Location {
    fn from(dto: UpdateLocationDto) -> Self {
        Location { id: dto.id, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name, address: dto.address, latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<&UpdateLocationDto> for Location {
    fn from(dto: &UpdateLocationDto) -> Self {
        Location { id: dto.id, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name.clone(), address: dto.address.clone(), latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
impl From<Location> for UpdateLocationDto {
    fn from(e: Location) -> Self {
        UpdateLocationDto { id: e.id, created_at: e.created_at, updated_at: e.updated_at, name: e.name, address: e.address, latitude: e.latitude, longitude: e.longitude, capacity: e.capacity, manager: e.manager }
    }
}
impl From<LocationDto> for UpdateLocationDto {
    fn from(dto: LocationDto) -> Self {
        UpdateLocationDto { id: dto.id, created_at: dto.created_at, updated_at: dto.updated_at, name: dto.name, address: dto.address, latitude: dto.latitude, longitude: dto.longitude, capacity: dto.capacity, manager: dto.manager }
    }
}
