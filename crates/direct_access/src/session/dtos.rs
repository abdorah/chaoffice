// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::Session;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct SessionDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub token: String,
    pub expires_at: chrono::DateTime<chrono::Utc>,
}

impl From<SessionDto> for Session {
    fn from(dto: SessionDto) -> Self {
        Session {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            token: dto.token,
            expires_at: dto.expires_at,
        }
    }
}

impl From<&SessionDto> for Session {
    fn from(dto: &SessionDto) -> Self {
        Session {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            token: dto.token.clone(),
            expires_at: dto.expires_at.clone(),
        }
    }
}

impl From<Session> for SessionDto {
    fn from(entity: Session) -> Self {
        SessionDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            token: entity.token,
            expires_at: entity.expires_at,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateSessionDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub token: String,
    pub expires_at: chrono::DateTime<chrono::Utc>,
}

impl From<CreateSessionDto> for Session {
    fn from(dto: CreateSessionDto) -> Self {
        Session {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            token: dto.token,
            expires_at: dto.expires_at,
        }
    }
}

impl From<&CreateSessionDto> for Session {
    fn from(dto: &CreateSessionDto) -> Self {
        Session {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            token: dto.token.clone(),
            expires_at: dto.expires_at.clone(),
        }
    }
}

impl From<Session> for CreateSessionDto {
    fn from(entity: Session) -> Self {
        CreateSessionDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            token: entity.token,
            expires_at: entity.expires_at,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateSessionDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub token: String,
    pub expires_at: chrono::DateTime<chrono::Utc>,
}

impl From<UpdateSessionDto> for Session {
    fn from(dto: UpdateSessionDto) -> Self {
        Session {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            token: dto.token,
            expires_at: dto.expires_at,
        }
    }
}

impl From<&UpdateSessionDto> for Session {
    fn from(dto: &UpdateSessionDto) -> Self {
        Session {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            token: dto.token.clone(),
            expires_at: dto.expires_at.clone(),
        }
    }
}

impl From<Session> for UpdateSessionDto {
    fn from(entity: Session) -> Self {
        UpdateSessionDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            token: entity.token,
            expires_at: entity.expires_at,
        }
    }
}

impl From<SessionDto> for UpdateSessionDto {
    fn from(dto: SessionDto) -> Self {
        UpdateSessionDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            token: dto.token,
            expires_at: dto.expires_at,
        }
    }
}
