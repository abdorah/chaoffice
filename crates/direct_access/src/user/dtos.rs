// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::User;
pub use common::entities::UserRole;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UserDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub username: String,
    pub password_hash: String,
    pub display_name: String,
    pub role: UserRole,
    pub is_active: bool,
    pub person: Option<EntityId>,
    pub session: Option<EntityId>,
}

impl From<UserDto> for User {
    fn from(dto: UserDto) -> Self {
        User {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            username: dto.username,
            password_hash: dto.password_hash,
            display_name: dto.display_name,
            role: dto.role,
            is_active: dto.is_active,
            person: dto.person,
            session: dto.session,
        }
    }
}

impl From<&UserDto> for User {
    fn from(dto: &UserDto) -> Self {
        User {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            username: dto.username.clone(),
            password_hash: dto.password_hash.clone(),
            display_name: dto.display_name.clone(),
            role: dto.role.clone(),
            is_active: dto.is_active.clone(),
            person: dto.person.clone(),
            session: dto.session.clone(),
        }
    }
}

impl From<User> for UserDto {
    fn from(entity: User) -> Self {
        UserDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            username: entity.username,
            password_hash: entity.password_hash,
            display_name: entity.display_name,
            role: entity.role,
            is_active: entity.is_active,
            person: entity.person,
            session: entity.session,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateUserDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub username: String,
    pub password_hash: String,
    pub display_name: String,
    pub role: UserRole,
    pub is_active: bool,
    pub person: Option<EntityId>,
    pub session: Option<EntityId>,
}

impl From<CreateUserDto> for User {
    fn from(dto: CreateUserDto) -> Self {
        User {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            username: dto.username,
            password_hash: dto.password_hash,
            display_name: dto.display_name,
            role: dto.role,
            is_active: dto.is_active,
            person: dto.person,
            session: dto.session,
        }
    }
}

impl From<&CreateUserDto> for User {
    fn from(dto: &CreateUserDto) -> Self {
        User {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            username: dto.username.clone(),
            password_hash: dto.password_hash.clone(),
            display_name: dto.display_name.clone(),
            role: dto.role.clone(),
            is_active: dto.is_active.clone(),
            person: dto.person.clone(),
            session: dto.session.clone(),
        }
    }
}

impl From<User> for CreateUserDto {
    fn from(entity: User) -> Self {
        CreateUserDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            username: entity.username,
            password_hash: entity.password_hash,
            display_name: entity.display_name,
            role: entity.role,
            is_active: entity.is_active,
            person: entity.person,
            session: entity.session,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateUserDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub username: String,
    pub password_hash: String,
    pub display_name: String,
    pub role: UserRole,
    pub is_active: bool,
}

impl From<UpdateUserDto> for User {
    fn from(dto: UpdateUserDto) -> Self {
        User {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            username: dto.username,
            password_hash: dto.password_hash,
            display_name: dto.display_name,
            role: dto.role,
            is_active: dto.is_active,
            person: Default::default(),
            session: Default::default(),
        }
    }
}

impl From<&UpdateUserDto> for User {
    fn from(dto: &UpdateUserDto) -> Self {
        User {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            username: dto.username.clone(),
            password_hash: dto.password_hash.clone(),
            display_name: dto.display_name.clone(),
            role: dto.role.clone(),
            is_active: dto.is_active.clone(),
            person: Default::default(),
            session: Default::default(),
        }
    }
}

impl From<User> for UpdateUserDto {
    fn from(entity: User) -> Self {
        UpdateUserDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            username: entity.username,
            password_hash: entity.password_hash,
            display_name: entity.display_name,
            role: entity.role,
            is_active: entity.is_active,
        }
    }
}

impl From<UserDto> for UpdateUserDto {
    fn from(dto: UserDto) -> Self {
        UpdateUserDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            username: dto.username,
            password_hash: dto.password_hash,
            display_name: dto.display_name,
            role: dto.role,
            is_active: dto.is_active,
        }
    }
}
pub use common::direct_access::user::UserRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UserRelationshipDto {
    pub id: EntityId,
    pub field: UserRelationshipField,
    pub right_ids: Vec<EntityId>,
}
