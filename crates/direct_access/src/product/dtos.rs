// Generated by Qleany v1.5.5 from entity_dtos.tera

use common::entities::Product;
pub use common::entities::ProductStatus;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct ProductDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub reference: String,
    pub description: String,
    pub quantity: i64,
    pub price_unit: f64,
    pub status: ProductStatus,
    pub category: Option<EntityId>,
    pub supplier: Option<EntityId>,
    pub location: Option<EntityId>,
}

impl From<ProductDto> for Product {
    fn from(dto: ProductDto) -> Self {
        Product {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            reference: dto.reference,
            description: dto.description,
            quantity: dto.quantity,
            price_unit: dto.price_unit,
            status: dto.status,
            category: dto.category,
            supplier: dto.supplier,
            location: dto.location,
        }
    }
}

impl From<&ProductDto> for Product {
    fn from(dto: &ProductDto) -> Self {
        Product {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            reference: dto.reference.clone(),
            description: dto.description.clone(),
            quantity: dto.quantity.clone(),
            price_unit: dto.price_unit.clone(),
            status: dto.status.clone(),
            category: dto.category.clone(),
            supplier: dto.supplier.clone(),
            location: dto.location.clone(),
        }
    }
}

impl From<Product> for ProductDto {
    fn from(entity: Product) -> Self {
        ProductDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            reference: entity.reference,
            description: entity.description,
            quantity: entity.quantity,
            price_unit: entity.price_unit,
            status: entity.status,
            category: entity.category,
            supplier: entity.supplier,
            location: entity.location,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateProductDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub reference: String,
    pub description: String,
    pub quantity: i64,
    pub price_unit: f64,
    pub status: ProductStatus,
    pub category: Option<EntityId>,
    pub supplier: Option<EntityId>,
    pub location: Option<EntityId>,
}

impl From<CreateProductDto> for Product {
    fn from(dto: CreateProductDto) -> Self {
        Product {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            reference: dto.reference,
            description: dto.description,
            quantity: dto.quantity,
            price_unit: dto.price_unit,
            status: dto.status,
            category: dto.category,
            supplier: dto.supplier,
            location: dto.location,
        }
    }
}

impl From<&CreateProductDto> for Product {
    fn from(dto: &CreateProductDto) -> Self {
        Product {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            reference: dto.reference.clone(),
            description: dto.description.clone(),
            quantity: dto.quantity.clone(),
            price_unit: dto.price_unit.clone(),
            status: dto.status.clone(),
            category: dto.category.clone(),
            supplier: dto.supplier.clone(),
            location: dto.location.clone(),
        }
    }
}

impl From<Product> for CreateProductDto {
    fn from(entity: Product) -> Self {
        CreateProductDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            reference: entity.reference,
            description: entity.description,
            quantity: entity.quantity,
            price_unit: entity.price_unit,
            status: entity.status,
            category: entity.category,
            supplier: entity.supplier,
            location: entity.location,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct UpdateProductDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub name: String,
    pub reference: String,
    pub description: String,
    pub quantity: i64,
    pub price_unit: f64,
    pub status: ProductStatus,
}

impl From<UpdateProductDto> for Product {
    fn from(dto: UpdateProductDto) -> Self {
        Product {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            reference: dto.reference,
            description: dto.description,
            quantity: dto.quantity,
            price_unit: dto.price_unit,
            status: dto.status,
            category: Default::default(),
            supplier: Default::default(),
            location: Default::default(),
        }
    }
}

impl From<&UpdateProductDto> for Product {
    fn from(dto: &UpdateProductDto) -> Self {
        Product {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            name: dto.name.clone(),
            reference: dto.reference.clone(),
            description: dto.description.clone(),
            quantity: dto.quantity.clone(),
            price_unit: dto.price_unit.clone(),
            status: dto.status.clone(),
            category: Default::default(),
            supplier: Default::default(),
            location: Default::default(),
        }
    }
}

impl From<Product> for UpdateProductDto {
    fn from(entity: Product) -> Self {
        UpdateProductDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            name: entity.name,
            reference: entity.reference,
            description: entity.description,
            quantity: entity.quantity,
            price_unit: entity.price_unit,
            status: entity.status,
        }
    }
}

impl From<ProductDto> for UpdateProductDto {
    fn from(dto: ProductDto) -> Self {
        UpdateProductDto {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            name: dto.name,
            reference: dto.reference,
            description: dto.description,
            quantity: dto.quantity,
            price_unit: dto.price_unit,
            status: dto.status,
        }
    }
}
pub use common::direct_access::product::ProductRelationshipField;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ProductRelationshipDto {
    pub id: EntityId,
    pub field: ProductRelationshipField,
    pub right_ids: Vec<EntityId>,
}
